---
description: "Фоновые агенты Cursor - полное руководство по настройке и управлению"
globs: ["**/*"]
alwaysApply: true
---

# 🤖 Фоновые агенты Cursor - Полное руководство

## 🎯 ОСНОВНЫЕ ПОНЯТИЯ

### Что такое фоновые агенты:
- **Асинхронные агенты** - работают в удаленной среде без участия пользователя
- **Автоматизация задач** - исправление ошибок, реализация функций, рефакторинг
- **Изолированная среда** - Ubuntu машина с доступом в интернет
- **GitHub интеграция** - клонируют репозиторий, работают в отдельной ветке
- **Trunk-based Development** - работа с релизными ветками и стабилизацией
- **Feature Toggles** - управление функциональностью через переключатели

### Типы агентов:
1. **Background Agents** - фоновые агенты (основной тип)
2. **Terminal Agents** - агенты терминала
3. **Planning Agents** - агенты планирования
4. **Review Agents** - агенты ревью
5. **Stabilization Agents** - агенты стабилизации релизов
6. **Feature Toggle Agents** - агенты управления переключателями

## 🚀 НАСТРОЙКА И АКТИВАЦИЯ

### 1. Предварительные требования:
- **Cursor версия 1.5.11+** (проверить: `cursor --version`)
- **API ключ Cursor** (настроить в `.env.local`)
- **GitHub интеграция** (права чтения/записи к репозиторию)
- **Тарифный план** с поддержкой агентов (Pro, Pro Plus, Ultra)

### 2. Активация через терминал:
```bash
# Проверка версии
cursor --version

# Активация агентов
./.cursor/activate-background-agents.sh

# Проверка статуса
./.cursor/monitor-agents.sh
```

### 3. Активация через IDE:
- **Ctrl+E** - режим фонового агента
- **Боковая панель** - вкладка "Agents"
- **Веб-интерфейс** - https://cursor.com/agents

## 🔧 КОНФИГУРАЦИЯ

### 1. Environment.json (обязательно):
```json
{
  "snapshot": "POPULATED_FROM_SETTINGS",
  "install": "npm install",
  "terminals": [
    {
      "name": "Run Next.js",
      "command": "npm run dev"
    }
  ]
}
```

### 2. Agent.json (правила агентов):
```json
{
  "agentConfig": {
    "background_agents": {
      "test_fixer": {
        "enabled": true,
        "triggers": ["test_failure", "typescript_error"],
        "actions": ["fix_import_errors", "fix_type_errors"]
      },
      "stabilization_agent": {
        "enabled": true,
        "triggers": ["release_branch_created", "stabilization_started"],
        "actions": ["run_full_tests", "fix_critical_bugs", "update_documentation"]
      },
      "feature_toggle_agent": {
        "enabled": true,
        "triggers": ["feature_toggle_error", "toggle_configuration_changed"],
        "actions": ["fix_toggle_config", "update_toggle_status", "validate_toggles"]
      },
      "trunk_dev_agent": {
        "enabled": true,
        "triggers": ["branch_workflow", "release_preparation"],
        "actions": ["create_feature_branch", "stabilize_release", "merge_to_main"]
      }
    }
  }
}
```

### 3. Cursor Rules (.cursor/rules/):
- **Project Rules** - правила проекта
- **User Rules** - глобальные правила
- **AGENTS.md** - простые инструкции

## 📊 УПРАВЛЕНИЕ АГЕНТАМИ

### 1. Запуск агентов:
```bash
# Через терминал
cursor agent launch --task "исправить ошибки тестов"

# Через IDE
Ctrl+E → выбрать агента → отправить задачу
```

### 2. Мониторинг статуса:
- **"On This Computer"** - активные агенты
- **"In Background"** - фоновые агенты
- **Время работы** - показывает активность
- **Процент прогресса** - показывает выполнение

### 3. Команды управления:
```bash
# Список агентов
cursor agent list

# Статус агента
cursor agent status <agent-id>

# Остановка агента
cursor agent stop <agent-id>

# Логи агента
cursor agent logs <agent-id>
```

## 🔌 API И ИНТЕГРАЦИИ

### 1. REST API:
- **Base URL**: `https://api.cursor.com/v1/agents`
- **Аутентификация**: Bearer token (CURSOR_API_KEY)
- **Endpoints**:
  - `GET /agents` - список агентов
  - `POST /agents` - запуск агента
  - `GET /agents/{id}` - статус агента
  - `DELETE /agents/{id}` - остановка агента

### 2. Webhooks:
- **URL**: настраивается в настройках агента
- **События**: запуск, завершение, ошибка
- **Формат**: JSON с метаданными агента

### 3. Slack интеграция:
- **Установка**: Cursor Settings → Integrations → Slack
- **Использование**: `@Cursor исправить ошибки тестов`
- **Управление**: контекстное меню в Slack

## 🎯 ПРАВИЛА РАБОТЫ С АГЕНТАМИ

### 1. Всегда используй @-symbols:
```
@background-agents @agent.json @environment.json @.cursor/rules/ @trunk-based-development @git-flow-strategy
```

### 2. Приоритеты задач (Trunk-based Development):
1. **Критические** - ошибки компиляции, тесты, стабилизация main
2. **Важные** - стабилизация релизных веток, feature toggles
3. **Желательные** - рефакторинг, оптимизация, документация
4. **Feature Toggles** - управление переключателями функций
5. **Release Management** - работа с релизными ветками

### 3. Мониторинг:
- **Проверяй статус** каждые 5 минут
- **Логируй действия** агентов
- **Отслеживай прогресс** через проценты

### 4. Безопасность:
- **Не коммить** API ключи
- **Проверяй изменения** перед мержем
- **Используй ветки** для экспериментов
- **Стабилизация** - только через релизные ветки
- **Feature Toggles** - для нестабильных функций

### 5. Trunk-based Development правила:
- **main** - только стабильные релизы (БЕЗ БАГОВ ВСЕГДА)
- **release/vX.Y** - стабилизация конкретных версий
- **develop** - активная разработка новых функций
- **feature/*** - отдельные задачи и функции
- **Feature Toggles** - управление функциональностью в продакшене

## 🚨 УСТРАНЕНИЕ НЕПОЛАДОК

### 1. Агенты не запускаются:
```bash
# Проверь API ключ
echo $CURSOR_API_KEY

# Проверь версию
cursor --version

# Перезапусти Cursor
cursor --restart
```

### 2. Агенты не видны в IDE:
- **Очисти кеш** Cursor
- **Перезапусти IDE**
- **Проверь настройки** агентов

### 3. Ошибки аутентификации:
- **Обнови API ключ** в `.env.local`
- **Проверь права** GitHub
- **Проверь тарифный план**

## 📈 МОНИТОРИНГ И АНАЛИТИКА

### 1. Статистика использования:
- **Время работы** агентов
- **Количество задач** выполнено
- **Успешность** выполнения
- **Стоимость** использования

### 2. Логи и отчеты:
```bash
# Логи агентов
cursor agent logs

# Статистика
./.cursor/monitor-agents.sh

# Отчеты
./.cursor/update-agent-status.sh
```

### 3. Алерты и уведомления:
- **Slack** - уведомления о завершении
- **Email** - отчеты о работе
- **Webhooks** - интеграция с внешними системами

## 💡 ЛУЧШИЕ ПРАКТИКИ

### 1. Планирование задач:
- **Четкие инструкции** для агентов
- **Конкретные цели** и метрики
- **Временные рамки** выполнения

### 2. Управление ресурсами:
- **Лимиты расходов** на агентов
- **Приоритизация** задач
- **Мониторинг** использования

### 3. Качество кода:
- **Автоматические тесты** перед мержем
- **Code review** изменений агентов
- **Документирование** изменений

## 🔄 АВТОМАТИЗАЦИЯ

### 1. Автоматические исправления:
- **Test Fixer Agent** - исправляет ошибки тестов
- **TypeScript Fixer Agent** - исправляет ошибки типов
- **Jest Configurator Agent** - настраивает Jest
- **GitHub Actions Monitor Agent** - мониторит CI/CD
- **Stabilization Agent** - стабилизирует релизные ветки
- **Feature Toggle Agent** - управляет переключателями функций
- **Trunk Dev Agent** - управляет workflow веток

### 2. Триггеры:
- **test_failure** - падение тестов
- **typescript_error** - ошибки TypeScript
- **github_actions_failure** - падение CI/CD
- **compilation_error** - ошибки компиляции
- **release_branch_created** - создание релизной ветки
- **stabilization_started** - начало стабилизации
- **feature_toggle_error** - ошибка feature toggle
- **toggle_configuration_changed** - изменение конфигурации toggle
- **branch_workflow** - workflow работы с ветками
- **release_preparation** - подготовка к релизу

### 3. Действия:
- **fix_import_errors** - исправление импортов
- **fix_type_errors** - исправление типов
- **update_test_configuration** - обновление конфигурации
- **analyze_workflow_logs** - анализ логов
- **run_full_tests** - запуск полного набора тестов
- **fix_critical_bugs** - исправление критических багов
- **update_documentation** - обновление документации
- **fix_toggle_config** - исправление конфигурации toggle
- **update_toggle_status** - обновление статуса toggle
- **validate_toggles** - валидация переключателей
- **create_feature_branch** - создание feature ветки
- **stabilize_release** - стабилизация релиза
- **merge_to_main** - мерж в main

## 🌳 TRUNK-BASED DEVELOPMENT ИНТЕГРАЦИЯ

### 1. Workflow агентов:
```bash
# Создание feature ветки
cursor agent launch --task "создать feature ветку для новой функции"

# Стабилизация релизной ветки
cursor agent launch --task "стабилизировать релизную ветку v1.2.0"

# Мерж в main после стабилизации
cursor agent launch --task "мерж стабилизированной ветки в main"
```

### 2. Структура веток для агентов:
- **main** - только стабильные релизы (агенты НЕ работают напрямую)
- **release/vX.Y** - стабилизация (агенты работают активно)
- **develop** - разработка (агенты создают feature ветки)
- **feature/*** - отдельные задачи (агенты реализуют функции)

### 3. Правила для агентов:
- **НЕ мержить** напрямую в main
- **Стабилизировать** только в релизных ветках
- **Использовать Feature Toggles** для нестабильных функций
- **Тестировать** все изменения перед мержем

## 🎛️ FEATURE TOGGLES ИНТЕГРАЦИЯ

### 1. Hot Toggles (горячие переключатели):
- **Время отклика**: Мгновенно (без перезапуска)
- **Источник**: База данных Supabase
- **Управление**: через агентов или админ панель
- **Примеры**: `new-ai-features`, `premium-subscription`, `advanced-analytics`

### 2. Cold Toggles (холодные переключатели):
- **Время отклика**: При следующем деплое
- **Источник**: Environment variables
- **Управление**: через конфигурацию
- **Примеры**: `debug-mode`, `experimental-ui`, `performance-monitoring`

### 3. Агенты для Feature Toggles:
```bash
# Включить toggle
cursor agent launch --task "включить toggle new-ai-features"

# Отключить toggle
cursor agent launch --task "отключить toggle beta-features"

# Валидировать все toggles
cursor agent launch --task "валидировать конфигурацию feature toggles"
```

### 4. Автоматические действия:
- **При ошибке toggle** - автоматическое отключение
- **При изменении конфигурации** - валидация и обновление
- **При релизе** - проверка совместимости toggles

## 🚀 РЕЛИЗНЫЕ АГЕНТЫ

### 1. Stabilization Agent:
- **Триггеры**: `release_branch_created`, `stabilization_started`
- **Действия**: 
  - Запуск полного набора тестов
  - Исправление критических багов
  - Обновление документации
  - Валидация Feature Toggles

### 2. Release Management Agent:
- **Триггеры**: `release_preparation`, `finalize_release`
- **Действия**:
  - Создание релизных веток
  - Синхронизация между ветками
  - Создание тегов релизов
  - Управление rollback

### 3. Feature Toggle Agent:
- **Триггеры**: `feature_toggle_error`, `toggle_configuration_changed`
- **Действия**:
  - Исправление конфигурации toggle
  - Обновление статуса toggle
  - Валидация переключателей
  - Мониторинг состояния

## 📊 МОНИТОРИНГ И АЛЕРТЫ

### 1. Ключевые метрики:
- **Время стабилизации** - от develop до релизной ветки
- **Количество релизных веток** - активные релизы
- **Feature toggle статус** - включенные/выключенные функции
- **Время отката** - скорость переключения между версиями
- **Стабильность main** - отсутствие багов в продакшене

### 2. Алерты:
- **Критический баг в main** - немедленное уведомление
- **Feature toggle ошибка** - проблемы с переключателями
- **Нестабильная релизная ветка** - много багов в стабилизации
- **Долгая стабилизация** - превышение времени релиза

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

### 1. Документация:
- **Официальная**: https://docs.cursor.com/en/background-agent
- **API**: https://docs.cursor.com/en/background-agent/api/overview
- **Интеграции**: https://docs.cursor.com/en/integrations/slack

### 2. Команды:
- **CLI**: https://docs.cursor.com/ru/cli/overview
- **Shell Mode**: https://docs.cursor.com/ru/cli/shell-mode
- **Headless**: https://docs.cursor.com/ru/cli/headless

### 3. Примеры:
- **Code Review**: https://docs.cursor.com/ru/cli/cookbook/code-review
- **Update Docs**: https://docs.cursor.com/ru/cli/cookbook/update-docs
- **Fix CI**: https://docs.cursor.com/ru/cli/cookbook/fix-ci

---

**Помни**: Фоновые агенты - это мощный инструмент автоматизации. Используй их эффективно, но всегда контролируй результат! 🚀