---
description: Обязательные правила использования единого фреймворка тестирования
globs: ["**/*.test.ts", "**/*.test.tsx", "**/*.spec.ts", "**/*.spec.tsx"]
alwaysApply: true
---

# 🧪 Фреймворк тестирования - ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА

## 🚨 КРИТИЧЕСКИ ВАЖНО

**ВСЕ ТЕСТЫ В ПРОЕКТЕ ДОЛЖНЫ ИСПОЛЬЗОВАТЬ ЕДИНЫЙ ФРЕЙМВОРК ТЕСТИРОВАНИЯ!**

Никаких исключений! Никаких альтернатив! Только единый фреймворк!

## 🎯 ОСНОВНЫЕ ПРИНЦИПЫ

### Единый фреймворк тестирования:
- **TestFramework** - основной фреймворк с конфигурацией
- **TestLogger** - единая система логирования
- **TestMocks** - централизованные моки
- **TestUtils** - общие утилиты для тестов

### Обязательное использование:
```typescript
import { testFramework, testLogger, testMocks, testUtils } from '@/tests/framework'
```

## 📋 ПРАВИЛА НАПИСАНИЯ ТЕСТОВ

### 1. Структура тестов:
```typescript
import { testFramework, testLogger, testMocks, testUtils } from '@/tests/framework'

describe('ComponentName', () => {
  beforeEach(() => {
    testMocks.setupAllMocks()
    testLogger.startTest('ComponentName')
  })

  afterEach(() => {
    testMocks.clearAllMocks()
    testLogger.endTest('ComponentName', true)
  })

  test('should work correctly', async () => {
    // Тест с использованием фреймворка
  })
})
```

### 2. Логирование:
```typescript
// Обязательно используй testLogger для всех операций
testLogger.info('TEST', 'Starting test operation')
testLogger.debug('MOCK', 'Setting up mock data', { data })
testLogger.error('TEST', 'Test failed', { error })
testLogger.performance('API_CALL', 150, 100) // операция, время, порог
```

### 3. Моки:
```typescript
// Используй testMocks для всех моков
testMocks.addUser(mockUser)
testMocks.addTask(mockTask)
testMocks.mockApiResponse('/api/test', { success: true })
```

### 4. Утилиты:
```typescript
// Используй testUtils для общих операций
const user = testUtils.generateUser({ name: 'Test User' })
await testUtils.waitForState(() => state.user, expectedUser)
const result = testUtils.renderWithProviders(<Component />)
```

## 🎨 ДЕКОРАТОРЫ И ХУКИ

### Декораторы для автоматизации:
```typescript
@TestSuite('UserManagement', TEST_CONFIGS.INTEGRATION, MOCK_CONFIGS.FULL)
class UserManagementTests {
  @TestCase('should create user', 'Creates a new user successfully')
  @WithMocks({ enableAuth: true, enableDatabase: true })
  async testCreateUser(context: TestContext) {
    // Тест автоматически получает context с logger, utils, config
  }

  @LogTest('should update user')
  @WithPerformance(100) // порог производительности
  async testUpdateUser() {
    // Тест автоматически логируется и измеряется производительность
  }
}
```

### Хуки для компонентов:
```typescript
function MyComponent() {
  const { logger, utils, config } = useTestFramework()
  
  // Использование в компоненте
  logger.debug('COMPONENT', 'Component rendered')
}
```

## 📊 КОНФИГУРАЦИИ

### Предустановленные конфигурации:
```typescript
import { TEST_CONFIGS, MOCK_CONFIGS } from '@/tests/framework'

// Unit тесты
testFramework.updateConfig(TEST_CONFIGS.UNIT)
testMocks.updateConfig(MOCK_CONFIGS.MINIMAL)

// Integration тесты
testFramework.updateConfig(TEST_CONFIGS.INTEGRATION)
testMocks.updateConfig(MOCK_CONFIGS.FULL)

// E2E тесты
testFramework.updateConfig(TEST_CONFIGS.E2E)
testMocks.updateConfig(MOCK_CONFIGS.API_ONLY)
```

### Быстрый старт:
```typescript
import { quickStart } from '@/tests/framework'

// Настройка для unit тестов
quickStart.unit()

// Настройка для integration тестов
quickStart.integration()

// Настройка для E2E тестов
quickStart.e2e()
```

## 🔧 УТИЛИТЫ ДЛЯ ТЕСТОВ

### Генерация тестовых данных:
```typescript
// Автоматическая генерация с переопределениями
const user = testUtils.generateUser({ name: 'Custom Name' })
const task = testUtils.generateTask({ priority: 'high' })
const subscription = testUtils.generateSubscription({ tier: 'premium' })
```

### Асинхронные операции:
```typescript
// Ожидание изменения состояния
await testUtils.waitForState(() => store.user, expectedUser)

// Ожидание появления элемента
const element = await testUtils.waitForElement(() => screen.getByText('Text'))

// Ожидание выполнения условия
await testUtils.waitForCondition(() => condition === true)
```

### Работа с производительностью:
```typescript
// Измерение производительности
const { result, duration } = await testUtils.measurePerformance(
  () => expensiveOperation(),
  'expensive_operation',
  100 // порог в мс
)
```

## 🎯 ПАТТЕРНЫ ТЕСТИРОВАНИЯ

### 1. Тест компонента:
```typescript
test('should render component', async () => {
  const { getByText } = testUtils.renderWithProviders(<Component />)
  
  testLogger.step('Rendering component')
  expect(getByText('Expected Text')).toBeInTheDocument()
  
  testLogger.assertion('Component rendered correctly', true)
})
```

### 2. Тест хука:
```typescript
test('should return user data', async () => {
  const mockUser = testUtils.generateUser()
  testMocks.mockAuthUser(mockUser)
  
  const { result } = renderHook(() => useAuth())
  
  await testUtils.waitForState(() => result.current.user, mockUser)
  
  testLogger.assertion('User data loaded', true, mockUser, result.current.user)
})
```

### 3. Тест API:
```typescript
test('should call API endpoint', async () => {
  testUtils.mockApiResponse('/api/users', { users: [] })
  
  const response = await fetch('/api/users')
  const data = await response.json()
  
  testLogger.api('/api/users', 'GET', 200, data)
  expect(data.users).toEqual([])
})
```

## 🚨 ОБРАБОТКА ОШИБОК

### Ожидание ошибок:
```typescript
test('should handle errors', async () => {
  await testUtils.expectToThrow(
    () => operationThatThrows(),
    'Expected error message'
  )
})
```

### Логирование ошибок:
```typescript
try {
  await riskyOperation()
} catch (error) {
  testLogger.error('OPERATION', 'Operation failed', { error })
  throw error
}
```

## 📈 МОНИТОРИНГ И ОТЧЕТЫ

### Логирование производительности:
```typescript
// Автоматическое логирование при превышении порога
@WithPerformance(100)
async function slowOperation() {
  // Операция будет автоматически измерена
}
```

### Экспорт в Allure:
```typescript
// Автоматический экспорт логов в Allure
const allureLogs = testLogger.exportToAllure()
```

## 🔄 МИГРАЦИЯ СУЩЕСТВУЮЩИХ ТЕСТОВ

### Шаг 1: Импорт фреймворка
```typescript
// Заменить
import { render, screen } from '@testing-library/react'

// На
import { testFramework, testLogger, testMocks, testUtils } from '@/tests/framework'
```

### Шаг 2: Обновление рендеринга
```typescript
// Заменить
const { getByText } = render(<Component />)

// На
const { getByText } = testUtils.renderWithProviders(<Component />)
```

### Шаг 3: Добавление логирования
```typescript
// Добавить в начало теста
testLogger.startTest('Test Name')

// Добавить в конец теста
testLogger.endTest('Test Name', true)
```

### Шаг 4: Использование моков
```typescript
// Заменить ручные моки
jest.mock('@/lib/auth')

// На
testMocks.setupAllMocks()
```

## 🎉 ЛУЧШИЕ ПРАКТИКИ

### 1. Всегда используй фреймворк:
- НЕ пиши тесты без использования фреймворка
- НЕ используй прямые вызовы console.log
- НЕ создавай моки вручную

### 2. Логируй все операции:
- Начинай тест с testLogger.startTest()
- Заканчивай тест с testLogger.endTest()
- Логируй важные шаги с testLogger.step()

### 3. Используй утилиты:
- Генерируй данные через testUtils.generate*()
- Ожидай изменения через testUtils.waitFor*()
- Измеряй производительность через testUtils.measurePerformance()

### 4. Настраивай моки:
- Используй testMocks.setupAllMocks() в beforeEach
- Используй testMocks.clearAllMocks() в afterEach
- Добавляй данные через testMocks.add*()

## 🚀 МАСШТАБИРОВАНИЕ

### Добавление новых типов тестов:
1. Создай новую конфигурацию в TEST_CONFIGS
2. Создай новый mock config в MOCK_CONFIGS
3. Добавь quickStart функцию
4. Обнови документацию

### Добавление новых утилит:
1. Добавь метод в TestUtils класс
2. Экспортируй в index.ts
3. Создай декоратор если нужно
4. Обнови правила

## 🚫 ЗАПРЕЩЕНО

### ❌ НЕ ДЕЛАЙ:
- НЕ пиши тесты без использования единого фреймворка
- НЕ используй прямые вызовы `render()`, `waitFor()`, `act()` из `@testing-library/react`
- НЕ используй `console.log`, `console.error`, `console.warn` в тестах
- НЕ создавай моки вручную через `jest.mock()`
- НЕ пиши тесты без `testLogger.startTest()` и `testLogger.endTest()`
- НЕ используй старые паттерны тестирования

### ✅ ОБЯЗАТЕЛЬНО ДЕЛАЙ:
- ВСЕГДА используй `testFramework`, `testLogger`, `testMocks`, `testUtils`
- ВСЕГДА начинай тест с `testLogger.startTest()`
- ВСЕГДА заканчивай тест с `testLogger.endTest()`
- ВСЕГДА используй `testUtils.renderWithProviders()` вместо `render()`
- ВСЕГДА используй `testUtils.waitForState()` вместо `waitFor()`
- ВСЕГДА используй `testUtils.act()` вместо `act()`
- ВСЕГДА используй `testMocks.setupAllMocks()` в `beforeEach`
- ВСЕГДА используй `testMocks.clearAllMocks()` в `afterEach`

## 🔍 ПРОВЕРКА СООТВЕТСТВИЯ

### Перед коммитом проверь:
1. Все ли тесты используют единый фреймворк?
2. Есть ли прямые вызовы `@testing-library/react`?
3. Есть ли `console.log` в тестах?
4. Настроены ли моки через `testMocks`?
5. Есть ли логирование через `testLogger`?

### Команды для проверки:
```bash
# Проверить соответствие фреймворку
npm run test:framework:check

# Запустить все тесты
npm test

# Проверить покрытие
npm run test:coverage
```

## 📋 ШАБЛОН НОВОГО ТЕСТА

```typescript
/**
 * 🧪 [Описание теста] - ОБЯЗАТЕЛЬНО используй единый фреймворк!
 */

import { testFramework, testLogger, testMocks, testUtils } from '@/tests/framework'
import { TEST_CONFIGS, MOCK_CONFIGS } from '@/tests/framework'

describe('ComponentName', () => {
  beforeEach(() => {
    // ОБЯЗАТЕЛЬНО: Настройка фреймворка
    testFramework.updateConfig(TEST_CONFIGS.UNIT) // или INTEGRATION, E2E
    testMocks.updateConfig(MOCK_CONFIGS.MINIMAL) // или FULL, API_ONLY
    testMocks.setupAllMocks()
    testLogger.startTest('ComponentName')
  })

  afterEach(() => {
    // ОБЯЗАТЕЛЬНО: Очистка
    testMocks.clearAllMocks()
    testLogger.endTest('ComponentName', true)
  })

  test('should work correctly', async () => {
    // ОБЯЗАТЕЛЬНО: Используй фреймворк
    const mockUser = testUtils.generateUser()
    testMocks.addUser(mockUser)
    
    const { getByText } = testUtils.renderWithProviders(<Component />)
    
    testLogger.step('Testing component')
    await testUtils.waitForElement(() => getByText('Expected Text'))
    
    testLogger.assertion('Component works correctly', true)
    expect(getByText('Expected Text')).toBeInTheDocument()
  })
})
```

## 🎯 ЦЕЛИ И ПРЕИМУЩЕСТВА

### Зачем единый фреймворк:
- **Консистентность**: все тесты выглядят одинаково
- **Масштабируемость**: легко добавлять новые тесты
- **Отладка**: единое логирование и мониторинг
- **Поддержка**: централизованное управление
- **Качество**: стандартизированные паттерны

### Что дает фреймворк:
- Автоматическое логирование всех операций
- Централизованные моки для всех систем
- Общие утилиты для рендеринга и асинхронных операций
- Декораторы для автоматизации
- Предустановленные конфигурации
- Интеграция с Allure и другими инструментами

---

**🚨 КРИТИЧЕСКИ ВАЖНО: Фреймворк тестирования - это не опция, а ОБЯЗАТЕЛЬНОСТЬ! Всегда используй его компоненты! 🧪**

## Шаблоны тестовых двойников (Test Doubles Patterns)

- **Тестовая заглушка (Test Stub)** – простой stub, возвращает заранее определённые данные без логики. Используйте для изоляции кода от зависимостей.
- **Тестовый агент (Test Spy)** – объект, оборачивающий реальный компонент для записи информации о вызовах (сколько раз, с какими аргументами).
- **Подставной объект (Mock Object)** – позволяет настроить ожидаемое поведение и проверять, какие методы были вызваны и с какими параметрами.
- **Поддельный объект (Fake Object)** – in-memory реализация зависимости со своей упрощённой логикой, приближённой к реальной, но без внешних эффектов.
- **Настраиваемый тестовый двойник (Configurable Test Double)** – комбинирует возможности stub и fake, позволяя в тестах конфигурировать ответы на разные условия.
- **Фиксированный тестовый двойник (Hard-Coded Test Double)** – stub с жёстко зашитыми значениями, не подконтрольный во время выполнения теста.
- **Подкласс, специфичный для теста (Test-Specific Subclass)** – наследник реального класса, реализующий или переопределяющий методы для целей конкретного теста.