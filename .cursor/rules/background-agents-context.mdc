---
description: "Контекст фоновых агентов для чата - всегда включать в работу"
globs: ["**/*"]
alwaysApply: true
---

# 🤖 КОНТЕКСТ ФОНОВЫХ АГЕНТОВ CURSOR

## 🎯 ВСЕГДА ВКЛЮЧАТЬ В РАБОТУ

### @-symbols для фоновых агентов:
- `@background-agents` - основные правила работы с агентами
- `@agent.json` - конфигурация агентов
- `@environment.json` - настройка окружения
- `@.cursor/rules/` - правила проекта
- `@AGENTS.md` - документация агентов
- `@trunk-based-development` - Trunk-based Development стратегия
- `@git-flow-strategy` - стратегия управления релизами
- `@src/lib/featureToggles.ts` - система Feature Toggles
- `@src/hooks/useFeatureToggle.ts` - React хуки для toggles

## 🚀 БЫСТРЫЙ СТАРТ

### 1. Активация агентов:
```bash
# Активировать агентов
./.cursor/activate-background-agents.sh

# Проверить статус
./.cursor/monitor-agents.sh

# Обновить статус
./.cursor/update-agent-status.sh
```

### 2. Мониторинг в IDE:
- **Ctrl+E** - режим фонового агента
- **Боковая панель** - вкладка "Agents"
- **"On This Computer"** - активные агенты
- **"In Background"** - фоновые агенты

### 3. Веб-интерфейс:
- **URL**: https://cursor.com/agents
- **Управление**: запуск, остановка, мониторинг
- **Интеграции**: Slack, GitHub, API

## 🔧 КОНФИГУРАЦИЯ

### 1. Environment.json:
```json
{
  "snapshot": "POPULATED_FROM_SETTINGS",
  "install": "npm install",
  "terminals": [
    {
      "name": "Run Next.js",
      "command": "npm run dev"
    }
  ]
}
```

### 2. Agent.json:
```json
{
  "agentConfig": {
    "background_agents": {
      "test_fixer": {
        "enabled": true,
        "triggers": ["test_failure", "typescript_error"],
        "actions": ["fix_import_errors", "fix_type_errors"]
      },
      "stabilization_agent": {
        "enabled": true,
        "triggers": ["release_branch_created", "stabilization_started"],
        "actions": ["run_full_tests", "fix_critical_bugs", "update_documentation"]
      },
      "feature_toggle_agent": {
        "enabled": true,
        "triggers": ["feature_toggle_error", "toggle_configuration_changed"],
        "actions": ["fix_toggle_config", "update_toggle_status", "validate_toggles"]
      },
      "trunk_dev_agent": {
        "enabled": true,
        "triggers": ["branch_workflow", "release_preparation"],
        "actions": ["create_feature_branch", "stabilize_release", "merge_to_main"]
      }
    }
  }
}
```

## 📊 УПРАВЛЕНИЕ

### 1. Команды терминала:
```bash
# Список агентов
cursor agent list

# Статус агента
cursor agent status <agent-id>

# Запуск агента
cursor agent launch --task "исправить ошибки тестов"

# Остановка агента
cursor agent stop <agent-id>

# Логи агента
cursor agent logs <agent-id>
```

### 2. API команды:
```bash
# REST API
curl -H "Authorization: Bearer $CURSOR_API_KEY" \
     https://api.cursor.com/v1/agents

# Webhooks
curl -X POST https://api.cursor.com/v1/agents/webhooks \
     -H "Authorization: Bearer $CURSOR_API_KEY" \
     -d '{"url": "https://your-webhook.com"}'
```

## 🎯 АВТОМАТИЧЕСКИЕ АГЕНТЫ

### 1. Test Fixer Agent:
- **Триггеры**: test_failure, typescript_error_in_tests, jest_error
- **Действия**: исправляет vitest→jest, типы, моки
- **Паттерны**: "Cannot find module 'vitest'", "Property 'toBeInTheDocument' does not exist"

### 2. TypeScript Fixer Agent:
- **Триггеры**: typescript_error, compilation_error, type_error
- **Действия**: исправляет типы, импорты, интерфейсы
- **Паттерны**: "Type 'null' is not assignable", "Property 'subscriptionStatus' is missing"

### 3. Jest Configurator Agent:
- **Триггеры**: jest_setup_error, testing_library_error, jest_dom_error
- **Действия**: настраивает Jest, Testing Library
- **Паттерны**: "vi is not defined", "vi.fn is not a function"

### 4. GitHub Actions Monitor Agent:
- **Триггеры**: github_actions_failure, workflow_error, ci_cd_error
- **Действия**: анализирует логи, исправляет конфигурацию
- **Паттерны**: "actions/upload-artifact@v3", "workflow failed"

### 5. Stabilization Agent:
- **Триггеры**: release_branch_created, stabilization_started
- **Действия**: стабилизирует релизные ветки, исправляет баги
- **Паттерны**: "stabilization started", "release branch created"

### 6. Feature Toggle Agent:
- **Триггеры**: feature_toggle_error, toggle_configuration_changed
- **Действия**: управляет переключателями функций
- **Паттерны**: "toggle error", "configuration changed"

### 7. Trunk Dev Agent:
- **Триггеры**: branch_workflow, release_preparation
- **Действия**: управляет workflow веток, создает релизы
- **Паттерны**: "feature branch", "release preparation"

## 🚨 УСТРАНЕНИЕ НЕПОЛАДОК

### 1. Агенты не запускаются:
```bash
# Проверить API ключ
echo $CURSOR_API_KEY

# Проверить версию
cursor --version

# Перезапустить Cursor
cursor --restart
```

### 2. Агенты не видны в IDE:
- Очистить кеш Cursor
- Перезапустить IDE
- Проверить настройки агентов

### 3. Ошибки аутентификации:
- Обновить API ключ в `.env.local`
- Проверить права GitHub
- Проверить тарифный план

## 📈 МОНИТОРИНГ

### 1. Статус агентов:
- **"On This Computer"** - активные агенты (время + процент)
- **"In Background"** - фоновые агенты (должны работать постоянно)
- **Время работы** - показывает активность
- **Процент прогресса** - показывает выполнение

### 2. Логи и отчеты:
```bash
# Логи агентов
cursor agent logs

# Статистика
./.cursor/monitor-agents.sh

# Отчеты
./.cursor/update-agent-status.sh
```

### 3. Алерты:
- **Slack** - уведомления о завершении
- **Email** - отчеты о работе
- **Webhooks** - интеграция с внешними системами

## 💡 ЛУЧШИЕ ПРАКТИКИ

### 1. Планирование задач:
- Четкие инструкции для агентов
- Конкретные цели и метрики
- Временные рамки выполнения

### 2. Управление ресурсами:
- Лимиты расходов на агентов
- Приоритизация задач
- Мониторинг использования

### 3. Качество кода:
- Автоматические тесты перед мержем
- Code review изменений агентов
- Документирование изменений

## 🔄 ИНТЕГРАЦИИ

### 1. Slack:
- Установка: Cursor Settings → Integrations → Slack
- Использование: `@Cursor исправить ошибки тестов`
- Управление: контекстное меню в Slack

### 2. GitHub:
- Права чтения/записи к репозиторию
- Клонирование в отдельную ветку
- Push изменений для передачи

### 3. API:
- REST API для управления агентами
- Webhooks для уведомлений
- Интеграция с внешними системами

## 🌳 TRUNK-BASED DEVELOPMENT

### 1. Структура веток:
- **main** - только стабильные релизы (БЕЗ БАГОВ ВСЕГДА)
- **release/vX.Y** - стабилизация конкретных версий
- **develop** - активная разработка новых функций
- **feature/*** - отдельные задачи и функции

### 2. Workflow агентов:
```bash
# Создание feature ветки
cursor agent launch --task "создать feature ветку для новой функции"

# Стабилизация релизной ветки
cursor agent launch --task "стабилизировать релизную ветку v1.2.0"

# Мерж в main после стабилизации
cursor agent launch --task "мерж стабилизированной ветки в main"
```

### 3. Правила для агентов:
- **НЕ мержить** напрямую в main
- **Стабилизировать** только в релизных ветках
- **Использовать Feature Toggles** для нестабильных функций
- **Тестировать** все изменения перед мержем

## 🎛️ FEATURE TOGGLES

### 1. Hot Toggles (горячие):
- **Время отклика**: Мгновенно (без перезапуска)
- **Источник**: База данных Supabase
- **Примеры**: `new-ai-features`, `premium-subscription`, `advanced-analytics`

### 2. Cold Toggles (холодные):
- **Время отклика**: При следующем деплое
- **Источник**: Environment variables
- **Примеры**: `debug-mode`, `experimental-ui`, `performance-monitoring`

### 3. Агенты для Feature Toggles:
```bash
# Включить toggle
cursor agent launch --task "включить toggle new-ai-features"

# Отключить toggle
cursor agent launch --task "отключить toggle beta-features"

# Валидировать все toggles
cursor agent launch --task "валидировать конфигурацию feature toggles"
```

## 🚀 РЕЛИЗНЫЕ АГЕНТЫ

### 1. Stabilization Agent:
- **Триггеры**: `release_branch_created`, `stabilization_started`
- **Действия**: 
  - Запуск полного набора тестов
  - Исправление критических багов
  - Обновление документации
  - Валидация Feature Toggles

### 2. Release Management Agent:
- **Триггеры**: `release_preparation`, `finalize_release`
- **Действия**:
  - Создание релизных веток
  - Синхронизация между ветками
  - Создание тегов релизов
  - Управление rollback

### 3. Feature Toggle Agent:
- **Триггеры**: `feature_toggle_error`, `toggle_configuration_changed`
- **Действия**:
  - Исправление конфигурации toggle
  - Обновление статуса toggle
  - Валидация переключателей
  - Мониторинг состояния

## 📊 МОНИТОРИНГ

### 1. Ключевые метрики:
- **Время стабилизации** - от develop до релизной ветки
- **Количество релизных веток** - активные релизы
- **Feature toggle статус** - включенные/выключенные функции
- **Время отката** - скорость переключения между версиями
- **Стабильность main** - отсутствие багов в продакшене

### 2. Алерты:
- **Критический баг в main** - немедленное уведомление
- **Feature toggle ошибка** - проблемы с переключателями
- **Нестабильная релизная ветка** - много багов в стабилизации
- **Долгая стабилизация** - превышение времени релиза

---

**Помни**: Всегда включай контекст фоновых агентов в работу! Используй @-symbols и следи за статусом агентов! 🚀

**Trunk-based Development + Feature Toggles + Фоновые агенты = максимальная автоматизация и стабильность!** 🌳🎛️🤖