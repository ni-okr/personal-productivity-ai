---
title: "MCP серверы - быстрая справка"
description: "Краткая справка по использованию MCP серверов для быстрого доступа"
priority: 4
tags: ["mcp", "quick", "reference", "commands", "aliases"]
alwaysApply: true
---

# 🔌 MCP серверы - быстрая справка

## 🚀 ОСНОВНЫЕ КОМАНДЫ

### GitHub MCP (30+ операций):
```bash
# Базовые операции
gstatus                    # Статус репозитория
gadd                      # Добавить все файлы
gcommit "message"         # Коммит с сообщением
gpush                     # Отправить в remote
gpull                     # Получить изменения
gbranch [name]            # Создать/показать ветки
gcheckout <name>          # Переключить ветку

# Продвинутые workflow
gflow "message"           # Полный workflow (add→commit→push)
gquick "message"          # Быстрый коммит без push
gsync                     # Синхронизация (pull→push)
gdev feature-name         # Управление сессиями разработки

# Управление
gbackup                   # Резервное копирование
gclean                    # Очистка репозитория
gstash [message]          # Сохранить изменения
gpop                      # Восстановить изменения
greset [mode] [target]    # Сброс состояния

# Специализированные
gtag [action] [name]      # Управление тегами
gmerge <branch>           # Слияние веток
grebase [target]          # Перебазирование
gcherry <commit>          # Cherry-pick коммита
gblame <file>             # Анализ авторства
gbisect [action] [commit] # Поиск багов
```

### Filesystem MCP:
- Чтение файлов и директорий
- Создание и редактирование файлов
- Поиск по содержимому
- Управление структурой проекта

### Supabase MCP:
- Выполнение SQL запросов
- Управление таблицами
- RLS политики
- Аутентификация

### Playwright MCP:
- Автоматизация браузера
- E2E тестирование
- Скриншоты
- Взаимодействие с элементами

### Firecrawl MCP:
- Скрапинг веб-страниц
- Извлечение контента
- Анализ структуры
- Batch обработка

### Time MCP:
- Текущее время
- Форматирование дат
- Конвертация временных зон
- Относительное время

### Memory MCP:
- Сохранение контекста
- Восстановление информации
- Управление знаниями

## 🎯 ПАТТЕРНЫ ИСПОЛЬЗОВАНИЯ

### Создание новой функции:
```bash
# 1. Создать ветку
gbranch feature-auth

# 2. Переключиться на ветку
gcheckout feature-auth

# 3. Разработать функцию (через Filesystem MCP)

# 4. Протестировать (через Playwright MCP)

# 5. Коммит и push
gflow "implement authentication feature"
```

### Исправление бага:
```bash
# 1. Проверить статус
gstatus

# 2. Исправить код (через Filesystem MCP)

# 3. Быстрый коммит
gquick "fix authentication bug"

# 4. Отправить изменения
gpush
```

### Управление сессией разработки:
```bash
# Начать сессию
gdev feature-auth

# Работать с кодом...

# Завершить сессию
gflow "complete authentication feature"
```

## 🔧 ПРОВЕРКА СТАТУСА

### Статус MCP серверов:
```bash
# Проверить работающие процессы
ps aux | grep mcp

# Проверить GitHub MCP
npx github-mcp-server --help

# Проверить Playwright MCP
npx @playwright/mcp --help
```

### Перезапуск:
```bash
# Перезапустить Cursor для активации MCP
# Или перезапустить отдельные серверы
```

## 📊 МОНИТОРИНГ

### Логи:
```bash
# Логи MCP серверов
tail -f ~/.cursor/logs/mcp.log

# Отладка GitHub MCP
DEBUG=mcp* npx github-mcp-server
```

### Метрики:
- GitHub: коммиты, PR, Issues, CI/CD статус
- Filesystem: созданные файлы, изменения
- Playwright: тесты, покрытие, время выполнения
- Supabase: запросы, производительность

## ⚡ БЫСТРЫЕ СОВЕТЫ

1. **Используй алиасы** - они быстрее полных команд
2. **Комбинируй операции** - gflow вместо отдельных команд
3. **Сохраняй контекст** - используй Memory MCP
4. **Мониторь статус** - регулярно проверяй работу серверов
5. **Используй workflow** - gdev для управления сессиями

---

**MCP серверы = суперсилы в разработке! 🚀**