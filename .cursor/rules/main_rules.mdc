---
alwaysApply: true
description: "Personal Productivity AI - Основные правила проекта"
priority: 1
tags: ["project", "rules", "architecture", "mvp"]
---



# 🧠 Personal Productivity AI - Контекст проекта для ИИ

## 🎯 МИССИЯ ПРОЕКТА
Создать MVP приложения для управления продуктивностью с ИИ-помощником, быстро запустить в продакшн для монетизации, затем поэтапно развивать в "LifeOS" - автоматизированную систему жизни.

## 🤖 ПРАВИЛА РАБОТЫ С ИИ АССИСТЕНТОМ

### 🎯 ОБЯЗАТЕЛЬНОЕ ИСПОЛЬЗОВАНИЕ @-SYMBOLS:
**Для каждой задачи ВСЕГДА включать контекст:**
- `@main_rules` - правила проекта (этот файл)
- `@src/lib/auth.ts` - система авторизации
- `@src/stores/useAppStore.ts` - глобальное состояние
- `@src/lib/supabase.ts` - база данных
- `@tests/` - все тесты проекта
- `@docs/` - документация архитектуры

### Приоритеты разработки:
1. **СТАБИЛЬНОСТЬ ПРЕЖДЕ ВСЕГО** - код должен работать без ошибок
2. **ТЕСТИРОВАНИЕ ОБЯЗАТЕЛЬНО** - каждая новая функция должна быть протестирована
3. **ДОКУМЕНТАЦИЯ АКТУАЛЬНА** - обновлять документацию при изменениях
4. **БЕЗОПАСНОСТЬ** - всегда проверять RLS, валидацию, санитизацию
5. **ПРОИЗВОДИТЕЛЬНОСТЬ** - оптимизировать для быстрой загрузки
6. **КОНТЕКСТНАЯ РАБОТА** - использовать @-symbols для полного понимания проекта
7. **GIT WORKFLOW** - после каждого завершенного шага создавать PR и мержить в main

### Стиль кодирования:
- Всегда использовать TypeScript с строгой типизацией
- Предпочитать функциональный стиль программирования
- Использовать Tailwind CSS для стилизации
- Следовать принципам DRY и SOLID
- Писать самодокументируемый код с понятными именами

### Тестирование:
- Unit тесты для всех утилит и бизнес-логики
- Integration тесты для API и внешних сервисов
- E2E тесты для критических пользовательских сценариев
- Использовать Allure для детальных отчетов

### Обработка ошибок:
- Всегда использовать try-catch для асинхронных операций
- Логировать ошибки с контекстом
- Показывать пользователю понятные сообщения об ошибках
- Использовать Error Boundaries для React компонентов

## 📊 ТЕКУЩИЙ СТАТУС MVP
- ✅ Базовая структура Next.js 14 + TypeScript + Tailwind
- ✅ Система задач с Zustand store
- ✅ ИИ планировщик (3 фазы: базовый UI, псевдо-ИИ алгоритмы, mock интеграция)
- ✅ Валидация форм и Error Boundaries для стабильности
- ✅ Система авторизации с Supabase Auth
- ✅ База данных (users, tasks, productivity_metrics, ai_suggestions)
- 🔄 В процессе: интеграция компонентов авторизации

## 🏗️ АРХИТЕКТУРА

### Технологический стек:
- **Frontend**: Next.js 14, React, TypeScript, Tailwind CSS
- **State Management**: Zustand
- **Database**: Supabase (PostgreSQL + Auth + RLS)
- **Deployment**: Vercel
- **AI Integration**: Mock → OpenAI GPT-4o Mini (Premium) → Multiple models

### Структура файлов:
```
src/
├── app/                    # Next.js App Router
│   ├── page.tsx           # Главная страница (лендинг + подписка)
│   ├── planner/           # ИИ планировщик задач
│   └── api/               # API routes (subscribe, test)
├── components/            # React компоненты
│   ├── ui/Button.tsx      # UI компоненты
│   ├── ErrorBoundary.tsx  # Обработка ошибок
│   └── MobileOptimized.tsx
├── lib/                   # Бизнес-логика
│   ├── supabase.ts        # Клиент Supabase
│   ├── auth.ts            # Система авторизации
│   ├── smartPlanning.ts   # Алгоритмы планирования
│   └── aiModels.ts        # ИИ модели и интеграции
├── stores/                # Zustand stores
│   └── useAppStore.ts     # Глобальное состояние
├── types/                 # TypeScript типы
├── utils/                 # Утилиты
│   ├── validation.ts      # Валидация данных
│   └── cn.ts              # Tailwind утилиты
└── services/              # Внешние сервисы
    └── api.ts             # HTTP клиент
```

## 🎨 ДИЗАЙН ПРИНЦИПЫ

### UI/UX:
- **Минимализм**: Чистый, современный интерфейс
- **Мобильность**: Mobile-first подход, PWA готовность
- **Доступность**: Семантическая разметка, keyboard navigation
- **Производительность**: Lazy loading, оптимизация изображений

### Цветовая схема:
- Primary: Indigo (bg-indigo-600, text-indigo-600)
- Success: Green (bg-green-500)
- Warning: Yellow (bg-yellow-500) 
- Danger: Red (bg-red-600)
- Neutral: Gray (bg-gray-100, text-gray-900)

## 🧠 ИИ КОНЦЕПЦИЯ

### Многоуровневая архитектура:
1. **Free tier**: Псевдо-ИИ (умные алгоритмы, без LLM)
2. **Premium**: OpenAI GPT-4o Mini интеграция
3. **Pro**: Multiple AI models (Claude, Gemini, GPT-4)
4. **Self-hosted**: Локальные модели на устройстве пользователя

### ИИ возможности:
- 📝 Автоматическое создание задач
- 🎯 Умная приоритизация
- 📊 Анализ продуктивности
- ⏰ Оптимизация расписания
- 💡 Персонализированные рекомендации

## 🚀 ROADMAP РАЗВИТИЯ

### Q4 2025 - MVP (ТЕКУЩИЙ ЭТАП):
- [x] Базовый планировщик задач
- [x] Система авторизации
- [ ] Интеграция компонентов входа/регистрации
- [ ] Сохранение задач в Supabase
- [ ] Один реальный ИИ API (OpenAI)
- [ ] Система подписок (Stripe)
- [ ] PWA доработка

### Q1 2026 - Псевдо-ИИ:
- [ ] Умные алгоритмы планирования
- [ ] Анализ паттернов продуктивности
- [ ] Автоматические рекомендации
- [ ] Интеграция с календарем

### Q2-Q3 2026 - Реальный ИИ:
- [ ] LLM интеграции (OpenAI, Claude, Gemini)
- [ ] Контекстуальные рекомендации
- [ ] Обработка естественного языка
- [ ] Персонализация на основе истории

### Q4 2026+ - LifeOS:
- [ ] Парсинг email для создания задач
- [ ] Анализ медицинских данных
- [ ] Планирование питания и покупок
- [ ] Интеграция с доставкой
- [ ] Контекстуальное планирование

## 💰 МОНЕТИЗАЦИЯ

### Тарифные планы:
- **Free**: Базовый функционал, псевдо-ИИ, до 50 задач
- **Premium** ($9.99/мес): OpenAI интеграция, до 500 задач, приоритет поддержки
- **Pro** ($19.99/мес): Все ИИ модели, неограниченные задачи, аналитика
- **Enterprise** ($49.99/мес): API доступ, интеграции, белый лейбл

### Дополнительные доходы:
- Комиссии с интеграций (доставка, календари)
- Партнерские программы (медицинские сервисы)
- Консультации по продуктивности

## 🔐 БЕЗОПАСНОСТЬ И ПРИВАТНОСТЬ

### Supabase настройки:
- Row Level Security (RLS) включен для всех таблиц
- Пользователи видят только свои данные
- Автоматические политики безопасности
- Шифрование данных в покое и при передаче

### Переменные окружения:
```env
NEXT_PUBLIC_SUPABASE_URL=https://zpgkzvflmgxrlgkecscg.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```
⚠️ НЕ КОММИТИТЬ в Git! Только в .env.local

## 🧪 ТЕСТИРОВАНИЕ

### Стратегия:
- Unit тесты для утилит и бизнес-логики
- Integration тесты для API routes
- E2E тесты для критических пользовательских сценариев
- Валидация всех форм и входных данных

### Инструменты:
- Jest для unit тестов
- Playwright для E2E тестов
- Supabase локальная разработка для тестирования БД

## 📱 PWA И МОБИЛЬНОСТЬ

### Возможности:
- Офлайн режим для просмотра задач
- Push уведомления для напоминаний
- Установка как нативное приложение
- Синхронизация при восстановлении соединения

### Файлы:
- `public/manifest.json` - PWA манифест
- `public/icon-*.png` - Иконки приложения
- Service Worker для кеширования

## 🎯 КЛЮЧЕВЫЕ ПРИНЦИПЫ РАЗРАБОТКИ

### Код:
- **TypeScript везде** - строгая типизация
- **Функциональный стиль** - избегать мутаций
- **Error boundaries** - graceful degradation
- **Валидация входных данных** - на клиенте и сервере
- **Responsive design** - mobile-first

### Производительность:
- **Lazy loading** компонентов
- **Оптимизация изображений** через Next.js Image
- **Минификация и сжатие** в продакшене
- **CDN** через Vercel Edge Network

### UX:
- **Loading states** для всех асинхронных операций
- **Error messages** понятные пользователю
- **Keyboard navigation** для доступности
- **Consistent spacing** через Tailwind

## 🔄 DEPLOYMENT И CI/CD

### Vercel настройки:
- Автоматический деплой из main ветки
- Preview deployments для PR
- Environment variables в Vercel dashboard
- Edge functions для API routes

### Домены:
- Production: personal-productivity-ai.vercel.app
- Preview: personal-productivity-qdsjkuxy.vercel.app (старый)

## 📚 ДОКУМЕНТАЦИЯ

### Файлы:
- `README.md` - Общее описание проекта
- `SUPABASE_SETUP.md` - Инструкции по настройке БД
- `.cursor/rules` - Контекст для ИИ (этот файл)

### API документация:
- `/api/subscribe` - POST подписка на рассылку
- `/api/test` - GET проверка работоспособности

## 🎨 КОМПОНЕНТЫ И ПАТТЕРНЫ

### Button компонент:
```tsx
<Button 
  variant="primary|secondary|ghost|danger|outline"
  size="sm|md|lg"
  isLoading={boolean}
  disabled={boolean}
>
  Текст кнопки
</Button>
```

### Валидация:
```tsx
import { validateTask, validateEmail } from '@/utils/validation'

const validation = validateTask(taskData)
if (!validation.isValid) {
  setErrors(validation.errors)
}
```

### Error Boundary:
```tsx
import { ErrorBoundary } from '@/components/ErrorBoundary'

<ErrorBoundary>
  <YourComponent />
</ErrorBoundary>
```

## 🏆 УРОКИ И BEST PRACTICES

### Что работает хорошо:
1. **Zustand** - простое и эффективное управление состоянием
2. **Supabase** - быстрая настройка авторизации и БД
3. **TypeScript** - ловит ошибки на этапе разработки
4. **Tailwind** - быстрая стилизация без CSS файлов
5. **Error Boundaries** - graceful обработка ошибок
6. **Валидация** - предотвращает некорректные данные

### Избегать:
1. **Мутации состояния** - использовать иммутабельные обновления
2. **Inline стили** - использовать Tailwind классы
3. **Хардкод значений** - выносить в константы/env
4. **Отсутствие loading states** - всегда показывать прогресс
5. **Игнорирование ошибок** - обрабатывать все catch блоки

### Паттерны именования:
- **Компоненты**: PascalCase (TaskCard, UserProfile)
- **Функции**: camelCase (handleSubmit, validateEmail)
- **Константы**: UPPER_SNAKE_CASE (API_BASE_URL)
- **Файлы**: kebab-case для страниц, PascalCase для компонентов

## 🎯 СЛЕДУЮЩИЕ ПРИОРИТЕТЫ

### Немедленно (для MVP):
1. Создать компоненты входа/регистрации
2. Интегрировать авторизацию с планировщиком
3. Сохранение задач в Supabase
4. Подключить OpenAI API для Premium
5. Настроить Stripe для подписок

## 🔧 ИНСТРУКЦИИ ПО CURSOR ДОКУМЕНТАЦИИ

### @-symbols (обязательно использовать):
- `@main_rules` - этот файл с правилами
- `@git-flow-strategy` - стратегия управления релизами
- `@background-agents` - фоновые агенты Cursor
- `@src/lib/auth.ts` - система авторизации Supabase
- `@src/stores/useAppStore.ts` - Zustand store
- `@src/lib/supabase.ts` - клиент базы данных
- `@src/lib/smartPlanning.ts` - алгоритмы планирования
- `@src/lib/aiModels.ts` - ИИ модели
- `@tests/` - все тесты (unit, integration, e2e)
- `@docs/` - документация архитектуры
- `@.cursor/mcp.json` - MCP серверы
- `@package.json` - зависимости проекта

### Agent Planning команды:
- `@agent plan:` - создать план выполнения задачи
- `@agent execute:` - выполнить план автоматически
- `@agent review:` - проанализировать прогресс

### Background Agents:
- **Test Fixer Agent** - исправляет ошибки тестов (vitest→jest, типы, моки)
- **TypeScript Fixer Agent** - исправляет ошибки TypeScript (импорты, типы, интерфейсы)
- **Jest Configurator Agent** - настраивает Jest конфигурацию (setup, testing-library)
- **GitHub Actions Monitor Agent** - мониторит CI/CD и исправляет ошибки workflow
- **Автоматические коммиты** после завершения задач
- **Запуск тестов** при изменениях кода
- **Обновление документации** и проверка качества кода

### Bugbot Pro (активен 14 дней):
- Автоматическая проверка PR на баги
- Inline комментарии с рекомендациями
- Проверка безопасности кода
- Анализ производительности

### Среднесрочно:
1. PWA доработка (офлайн, push)
2. Мобильная адаптация
3. Аналитика и метрики
4. A/B тестирование лендинга
5. SEO оптимизация

### Долгосрочно (LifeOS):
1. Email парсинг и автоматизация
2. Медицинские интеграции
3. Планирование питания
4. Интеграции доставки
5. Контекстуальный ИИ

---

## 💡 ФИЛОСОФИЯ ПРОЕКТА

**"От простого MVP к революционной LifeOS"**

Мы начинаем с минимально жизнеспособного продукта, который решает реальную проблему управления задачами. Каждая итерация добавляет ценность пользователям и приближает нас к видению полностью автоматизированной системы управления жизнью.

**Принципы:**
- 🎯 **Пользователь в центре** - каждая функция должна решать реальную проблему
- 🚀 **Быстрые итерации** - лучше выпустить несовершенное, чем не выпустить вообще
- 💰 **Устойчивая монетизация** - продукт должен окупать себя с первых пользователей
- 🧠 **ИИ как усилитель** - технология должна делать людей более продуктивными, а не заменять их
- 🔒 **Приватность прежде всего** - данные пользователей священны

**Помни**: Мы создаем не просто приложение, а платформу для улучшения качества жизни людей через умное планирование и автоматизацию рутины. 🌟

## 📋 ФОРМАТ ПРЕДЛОЖЕНИЯ СЛЕДУЮЩИХ ШАГОВ

### 🎯 ОБЯЗАТЕЛЬНЫЙ ФОРМАТ:
После завершения каждой задачи ВСЕГДА предлагать следующую приоритетную задачу в следующем формате:

```
## 🎉 Готов к работе!

**Следующая приоритетная задача:**

```
@main_rules @src/lib/auth.ts @src/stores/useAppStore.ts @src/lib/supabase.ts @tests/ @docs/

Создай полную систему авторизации с компонентами входа, регистрации и восстановления пароля, интегрированную с Supabase
```

**Начинаем работу!** 🚀
```

### 📝 ПРАВИЛА ФОРМАТИРОВАНИЯ:
1. **Заголовок**: "🎉 Готов к работе!" с эмодзи
2. **Вводный текст**: "Следующая приоритетная задача:"
3. **Задача**: В блоке кода с @-symbols и описанием
4. **Призыв к действию**: "Начинаем работу! 🚀"

### 🎯 ЦЕЛЬ ФОРМАТА:
- Четкое указание следующей приоритетной задачи
- Автоматическое включение @-symbols для контекста
- Прямой переход к выполнению без выбора
- Мотивирующий тон с эмодзи
- Готовый к копированию блок кода

### 🔄 ПРИОРИТЕТЫ ЗАДАЧ:
1. **Критические** - блокируют MVP (авторизация, база данных)
2. **Важные** - улучшают UX (UI компоненты, валидация)
3. **Желательные** - дополнительные функции (аналитика, PWA)
4. **Долгосрочные** - развитие продукта (LifeOS функции)


# 🧠 Personal Productivity AI - Контекст проекта для ИИ

## 🎯 МИССИЯ ПРОЕКТА
Создать MVP приложения для управления продуктивностью с ИИ-помощником, быстро запустить в продакшн для монетизации, затем поэтапно развивать в "LifeOS" - автоматизированную систему жизни.

## 🤖 ПРАВИЛА РАБОТЫ С ИИ АССИСТЕНТОМ

### 🎯 ОБЯЗАТЕЛЬНОЕ ИСПОЛЬЗОВАНИЕ @-SYMBOLS:
**Для каждой задачи ВСЕГДА включать контекст:**
- `@main_rules` - правила проекта (этот файл)
- `@src/lib/auth.ts` - система авторизации
- `@src/stores/useAppStore.ts` - глобальное состояние
- `@src/lib/supabase.ts` - база данных
- `@tests/` - все тесты проекта
- `@docs/` - документация архитектуры

### Приоритеты разработки:
1. **СТАБИЛЬНОСТЬ ПРЕЖДЕ ВСЕГО** - код должен работать без ошибок
2. **ТЕСТИРОВАНИЕ ОБЯЗАТЕЛЬНО** - каждая новая функция должна быть протестирована
3. **ДОКУМЕНТАЦИЯ АКТУАЛЬНА** - обновлять документацию при изменениях
4. **БЕЗОПАСНОСТЬ** - всегда проверять RLS, валидацию, санитизацию
5. **ПРОИЗВОДИТЕЛЬНОСТЬ** - оптимизировать для быстрой загрузки
6. **КОНТЕКСТНАЯ РАБОТА** - использовать @-symbols для полного понимания проекта
7. **GIT WORKFLOW** - после каждого завершенного шага создавать PR и мержить в main

### Стиль кодирования:
- Всегда использовать TypeScript с строгой типизацией
- Предпочитать функциональный стиль программирования
- Использовать Tailwind CSS для стилизации
- Следовать принципам DRY и SOLID
- Писать самодокументируемый код с понятными именами

### Тестирование:
- Unit тесты для всех утилит и бизнес-логики
- Integration тесты для API и внешних сервисов
- E2E тесты для критических пользовательских сценариев
- Использовать Allure для детальных отчетов

### Обработка ошибок:
- Всегда использовать try-catch для асинхронных операций
- Логировать ошибки с контекстом
- Показывать пользователю понятные сообщения об ошибках
- Использовать Error Boundaries для React компонентов

## 📊 ТЕКУЩИЙ СТАТУС MVP
- ✅ Базовая структура Next.js 14 + TypeScript + Tailwind
- ✅ Система задач с Zustand store
- ✅ ИИ планировщик (3 фазы: базовый UI, псевдо-ИИ алгоритмы, mock интеграция)
- ✅ Валидация форм и Error Boundaries для стабильности
- ✅ Система авторизации с Supabase Auth
- ✅ База данных (users, tasks, productivity_metrics, ai_suggestions)
- 🔄 В процессе: интеграция компонентов авторизации

## 🏗️ АРХИТЕКТУРА

### Технологический стек:
- **Frontend**: Next.js 14, React, TypeScript, Tailwind CSS
- **State Management**: Zustand
- **Database**: Supabase (PostgreSQL + Auth + RLS)
- **Deployment**: Vercel
- **AI Integration**: Mock → OpenAI GPT-4o Mini (Premium) → Multiple models

### Структура файлов:
```
src/
├── app/                    # Next.js App Router
│   ├── page.tsx           # Главная страница (лендинг + подписка)
│   ├── planner/           # ИИ планировщик задач
│   └── api/               # API routes (subscribe, test)
├── components/            # React компоненты
│   ├── ui/Button.tsx      # UI компоненты
│   ├── ErrorBoundary.tsx  # Обработка ошибок
│   └── MobileOptimized.tsx
├── lib/                   # Бизнес-логика
│   ├── supabase.ts        # Клиент Supabase
│   ├── auth.ts            # Система авторизации
│   ├── smartPlanning.ts   # Алгоритмы планирования
│   └── aiModels.ts        # ИИ модели и интеграции
├── stores/                # Zustand stores
│   └── useAppStore.ts     # Глобальное состояние
├── types/                 # TypeScript типы
├── utils/                 # Утилиты
│   ├── validation.ts      # Валидация данных
│   └── cn.ts              # Tailwind утилиты
└── services/              # Внешние сервисы
    └── api.ts             # HTTP клиент
```

## 🎨 ДИЗАЙН ПРИНЦИПЫ

### UI/UX:
- **Минимализм**: Чистый, современный интерфейс
- **Мобильность**: Mobile-first подход, PWA готовность
- **Доступность**: Семантическая разметка, keyboard navigation
- **Производительность**: Lazy loading, оптимизация изображений

### Цветовая схема:
- Primary: Indigo (bg-indigo-600, text-indigo-600)
- Success: Green (bg-green-500)
- Warning: Yellow (bg-yellow-500) 
- Danger: Red (bg-red-600)
- Neutral: Gray (bg-gray-100, text-gray-900)

## 🧠 ИИ КОНЦЕПЦИЯ

### Многоуровневая архитектура:
1. **Free tier**: Псевдо-ИИ (умные алгоритмы, без LLM)
2. **Premium**: OpenAI GPT-4o Mini интеграция
3. **Pro**: Multiple AI models (Claude, Gemini, GPT-4)
4. **Self-hosted**: Локальные модели на устройстве пользователя

### ИИ возможности:
- 📝 Автоматическое создание задач
- 🎯 Умная приоритизация
- 📊 Анализ продуктивности
- ⏰ Оптимизация расписания
- 💡 Персонализированные рекомендации

## 🚀 ROADMAP РАЗВИТИЯ

### Q4 2025 - MVP (ТЕКУЩИЙ ЭТАП):
- [x] Базовый планировщик задач
- [x] Система авторизации
- [ ] Интеграция компонентов входа/регистрации
- [ ] Сохранение задач в Supabase
- [ ] Один реальный ИИ API (OpenAI)
- [ ] Система подписок (Stripe)
- [ ] PWA доработка

### Q1 2026 - Псевдо-ИИ:
- [ ] Умные алгоритмы планирования
- [ ] Анализ паттернов продуктивности
- [ ] Автоматические рекомендации
- [ ] Интеграция с календарем

### Q2-Q3 2026 - Реальный ИИ:
- [ ] LLM интеграции (OpenAI, Claude, Gemini)
- [ ] Контекстуальные рекомендации
- [ ] Обработка естественного языка
- [ ] Персонализация на основе истории

### Q4 2026+ - LifeOS:
- [ ] Парсинг email для создания задач
- [ ] Анализ медицинских данных
- [ ] Планирование питания и покупок
- [ ] Интеграция с доставкой
- [ ] Контекстуальное планирование

## 💰 МОНЕТИЗАЦИЯ

### Тарифные планы:
- **Free**: Базовый функционал, псевдо-ИИ, до 50 задач
- **Premium** ($9.99/мес): OpenAI интеграция, до 500 задач, приоритет поддержки
- **Pro** ($19.99/мес): Все ИИ модели, неограниченные задачи, аналитика
- **Enterprise** ($49.99/мес): API доступ, интеграции, белый лейбл

### Дополнительные доходы:
- Комиссии с интеграций (доставка, календари)
- Партнерские программы (медицинские сервисы)
- Консультации по продуктивности

## 🔐 БЕЗОПАСНОСТЬ И ПРИВАТНОСТЬ

### Supabase настройки:
- Row Level Security (RLS) включен для всех таблиц
- Пользователи видят только свои данные
- Автоматические политики безопасности
- Шифрование данных в покое и при передаче

### Переменные окружения:
```env
NEXT_PUBLIC_SUPABASE_URL=https://zpgkzvflmgxrlgkecscg.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```
⚠️ НЕ КОММИТИТЬ в Git! Только в .env.local

## 🧪 ТЕСТИРОВАНИЕ

### Стратегия:
- Unit тесты для утилит и бизнес-логики
- Integration тесты для API routes
- E2E тесты для критических пользовательских сценариев
- Валидация всех форм и входных данных

### Инструменты:
- Jest для unit тестов
- Playwright для E2E тестов
- Supabase локальная разработка для тестирования БД

## 📱 PWA И МОБИЛЬНОСТЬ

### Возможности:
- Офлайн режим для просмотра задач
- Push уведомления для напоминаний
- Установка как нативное приложение
- Синхронизация при восстановлении соединения

### Файлы:
- `public/manifest.json` - PWA манифест
- `public/icon-*.png` - Иконки приложения
- Service Worker для кеширования

## 🎯 КЛЮЧЕВЫЕ ПРИНЦИПЫ РАЗРАБОТКИ

### Код:
- **TypeScript везде** - строгая типизация
- **Функциональный стиль** - избегать мутаций
- **Error boundaries** - graceful degradation
- **Валидация входных данных** - на клиенте и сервере
- **Responsive design** - mobile-first

### Производительность:
- **Lazy loading** компонентов
- **Оптимизация изображений** через Next.js Image
- **Минификация и сжатие** в продакшене
- **CDN** через Vercel Edge Network

### UX:
- **Loading states** для всех асинхронных операций
- **Error messages** понятные пользователю
- **Keyboard navigation** для доступности
- **Consistent spacing** через Tailwind

## 🔄 DEPLOYMENT И CI/CD

### Vercel настройки:
- Автоматический деплой из main ветки
- Preview deployments для PR
- Environment variables в Vercel dashboard
- Edge functions для API routes

### Домены:
- Production: personal-productivity-ai.vercel.app
- Preview: personal-productivity-qdsjkuxy.vercel.app (старый)

## 📚 ДОКУМЕНТАЦИЯ

### Файлы:
- `README.md` - Общее описание проекта
- `SUPABASE_SETUP.md` - Инструкции по настройке БД
- `.cursor/rules` - Контекст для ИИ (этот файл)

### API документация:
- `/api/subscribe` - POST подписка на рассылку
- `/api/test` - GET проверка работоспособности

## 🎨 КОМПОНЕНТЫ И ПАТТЕРНЫ

### Button компонент:
```tsx
<Button 
  variant="primary|secondary|ghost|danger|outline"
  size="sm|md|lg"
  isLoading={boolean}
  disabled={boolean}
>
  Текст кнопки
</Button>
```

### Валидация:
```tsx
import { validateTask, validateEmail } from '@/utils/validation'

const validation = validateTask(taskData)
if (!validation.isValid) {
  setErrors(validation.errors)
}
```

### Error Boundary:
```tsx
import { ErrorBoundary } from '@/components/ErrorBoundary'

<ErrorBoundary>
  <YourComponent />
</ErrorBoundary>
```

## 🏆 УРОКИ И BEST PRACTICES

### Что работает хорошо:
1. **Zustand** - простое и эффективное управление состоянием
2. **Supabase** - быстрая настройка авторизации и БД
3. **TypeScript** - ловит ошибки на этапе разработки
4. **Tailwind** - быстрая стилизация без CSS файлов
5. **Error Boundaries** - graceful обработка ошибок
6. **Валидация** - предотвращает некорректные данные

### Избегать:
1. **Мутации состояния** - использовать иммутабельные обновления
2. **Inline стили** - использовать Tailwind классы
3. **Хардкод значений** - выносить в константы/env
4. **Отсутствие loading states** - всегда показывать прогресс
5. **Игнорирование ошибок** - обрабатывать все catch блоки

### Паттерны именования:
- **Компоненты**: PascalCase (TaskCard, UserProfile)
- **Функции**: camelCase (handleSubmit, validateEmail)
- **Константы**: UPPER_SNAKE_CASE (API_BASE_URL)
- **Файлы**: kebab-case для страниц, PascalCase для компонентов

## 🎯 СЛЕДУЮЩИЕ ПРИОРИТЕТЫ

### Немедленно (для MVP):
1. Создать компоненты входа/регистрации
2. Интегрировать авторизацию с планировщиком
3. Сохранение задач в Supabase
4. Подключить OpenAI API для Premium
5. Настроить Stripe для подписок

## 🔧 ИНСТРУКЦИИ ПО CURSOR ДОКУМЕНТАЦИИ

### @-symbols (обязательно использовать):
- `@main_rules` - этот файл с правилами
- `@git-flow-strategy` - стратегия управления релизами
- `@background-agents` - фоновые агенты Cursor
- `@src/lib/auth.ts` - система авторизации Supabase
- `@src/stores/useAppStore.ts` - Zustand store
- `@src/lib/supabase.ts` - клиент базы данных
- `@src/lib/smartPlanning.ts` - алгоритмы планирования
- `@src/lib/aiModels.ts` - ИИ модели
- `@tests/` - все тесты (unit, integration, e2e)
- `@docs/` - документация архитектуры
- `@.cursor/mcp.json` - MCP серверы
- `@package.json` - зависимости проекта

### Agent Planning команды:
- `@agent plan:` - создать план выполнения задачи
- `@agent execute:` - выполнить план автоматически
- `@agent review:` - проанализировать прогресс

### Background Agents:
- **Test Fixer Agent** - исправляет ошибки тестов (vitest→jest, типы, моки)
- **TypeScript Fixer Agent** - исправляет ошибки TypeScript (импорты, типы, интерфейсы)
- **Jest Configurator Agent** - настраивает Jest конфигурацию (setup, testing-library)
- **GitHub Actions Monitor Agent** - мониторит CI/CD и исправляет ошибки workflow
- **Автоматические коммиты** после завершения задач
- **Запуск тестов** при изменениях кода
- **Обновление документации** и проверка качества кода

### Bugbot Pro (активен 14 дней):
- Автоматическая проверка PR на баги
- Inline комментарии с рекомендациями
- Проверка безопасности кода
- Анализ производительности

### Среднесрочно:
1. PWA доработка (офлайн, push)
2. Мобильная адаптация
3. Аналитика и метрики
4. A/B тестирование лендинга
5. SEO оптимизация

### Долгосрочно (LifeOS):
1. Email парсинг и автоматизация
2. Медицинские интеграции
3. Планирование питания
4. Интеграции доставки
5. Контекстуальный ИИ

---

## 💡 ФИЛОСОФИЯ ПРОЕКТА

**"От простого MVP к революционной LifeOS"**

Мы начинаем с минимально жизнеспособного продукта, который решает реальную проблему управления задачами. Каждая итерация добавляет ценность пользователям и приближает нас к видению полностью автоматизированной системы управления жизнью.

**Принципы:**
- 🎯 **Пользователь в центре** - каждая функция должна решать реальную проблему
- 🚀 **Быстрые итерации** - лучше выпустить несовершенное, чем не выпустить вообще
- 💰 **Устойчивая монетизация** - продукт должен окупать себя с первых пользователей
- 🧠 **ИИ как усилитель** - технология должна делать людей более продуктивными, а не заменять их
- 🔒 **Приватность прежде всего** - данные пользователей священны

**Помни**: Мы создаем не просто приложение, а платформу для улучшения качества жизни людей через умное планирование и автоматизацию рутины. 🌟


## 🔄 GIT FLOW STRATEGY - УПРАВЛЕНИЕ РЕЛИЗАМИ

### 🎯 НОВАЯ СТРАТЕГИЯ РЕЛИЗОВ (с 15.09.2025):

**Основные ветки:**
- `main` - **только стабильные релизы** (продакшн)
- `stabilization` - **ветка стабилизации** (тестирование и багфиксы)
- `develop` - **активная разработка** (новые функции)
- `feature/*` - **функциональные ветки** (отдельные задачи)

### 📋 WORKFLOW РАЗРАБОТКИ:

1. **Разработка функции:**
   ```bash
   git checkout develop
   git checkout -b feature/new-feature
   # ... разработка ...
   git add .
   git commit -m "feat: добавить новую функцию"
   git push origin feature/new-feature
   ```

2. **Мерж в develop:**
   - Создать PR: `feature/new-feature` → `develop`
   - Code review
   - Мерж после одобрения

3. **Подготовка к релизу:**
   ```bash
   git checkout develop
   git checkout -b stabilization/v1.2.0
   git push origin stabilization/v1.2.0
   ```

4. **Стабилизация (тестирование и багфиксы):**
   - Полное тестирование в ветке stabilization
   - Исправление найденных багов
   - Performance и security аудит

5. **Релиз в main:**
   - Создать PR: `stabilization/v1.2.0` → `main`
   - **Bugbot автоматический анализ**
   - Мерж после всех проверок
   - Создание тега релиза: `git tag v1.2.0`

### 🚫 ЗАПРЕЩЕНО:
- **Прямые коммиты в main** (кроме emergency hotfix)
- **Мерж develop в main** (только через stabilization)
- **Релиз без стабилизации** (обязательное тестирование)

### 🐛 BUGBOT АНАЛИЗ:

Bugbot проверяет:
- **Безопасность**: SQL injection, XSS, CSRF
- **Производительность**: медленные запросы, утечки памяти
- **Качество кода**: дублирование, сложность, покрытие тестами
- **Архитектура**: нарушение принципов SOLID, антипаттерны
- **TypeScript**: типизация, ошибки компиляции
- **React**: хуки, состояние, производительность

### 📊 ОТЧЕТ BUGBOT ВКЛЮЧАЕТ:

1. **Критические проблемы** (требуют немедленного исправления)
2. **Предупреждения** (рекомендуется исправить)
3. **Предложения** (улучшения качества кода)
4. **Метрики**: покрытие тестами, сложность, дублирование
5. **Рекомендации** по архитектуре и производительности

### ⚡ БЫСТРЫЕ КОМАНДЫ:

```bash
# Создать и запушить ветку
git checkout -b feature/task-name
git add .
git commit -m "feat: описание изменений"
git push origin feature/task-name

# После мержа - обновить main
git checkout main
git pull origin main
git branch -d feature/task-name
```

### 🎯 ПРИНЦИПЫ COMMIT MESSAGES:

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - документация
- `test:` - тесты
- `refactor:` - рефакторинг
- `perf:` - оптимизация производительности
- `chore:` - техническая работа

**Примеры:**
- `feat: добавить систему подписок с Stripe`
- `fix: исправить ошибку авторизации OAuth`
- `docs: обновить API документацию`
- `test: добавить unit тесты для auth`

## 🤖 ФОНОВЫЕ АГЕНТЫ ДЛЯ АВТОМАТИЧЕСКОГО ИСПРАВЛЕНИЯ ОШИБОК

### Активированные агенты:
- **Test Fixer Agent** 🧪 - автоматически исправляет ошибки тестов (vitest→jest, типы, моки)
- **TypeScript Fixer Agent** 🔧 - исправляет ошибки TypeScript (импорты, типы, интерфейсы)  
- **Jest Configurator Agent** ⚙️ - настраивает Jest конфигурацию (setup, testing-library)
- **GitHub Actions Monitor Agent** 🚀 - мониторит CI/CD и исправляет ошибки workflow

### Автоматические исправления:
- ✅ `vitest` → `jest` импорты и функции
- ✅ `null` → `undefined` для типов
- ✅ Исправление типов User и Task
- ✅ Исправление моков Supabase
- ✅ Настройка Testing Library
- ✅ Исправление GitHub Actions

### Активация:
```bash
./.cursor/activate-background-agents.sh
```

## 🔌 MCP СЕРВЕРЫ ДЛЯ АВТОМАТИЗАЦИИ РАЗРАБОТКИ

### Активные MCP серверы:
- **GitHub MCP** 🐙 - 30+ Git/GitHub операций (gstatus, gadd, gcommit, gpush, gflow)
- **Filesystem MCP** 📁 - работа с файлами проекта
- **Supabase MCP** 🗄️ - прямая работа с базой данных
- **Playwright MCP** 🎭 - E2E тестирование
- **Firecrawl MCP** 🕷️ - веб-скрапинг и анализ
- **Time MCP** ⏰ - работа с датами и временем
- **Memory MCP** 🧠 - сохранение контекста между сессиями

### Правила использования MCP:
1. **ВСЕГДА используй MCP серверы** вместо прямых команд
2. **GitHub операции**: gstatus, gadd, gcommit, gpush, gflow
3. **Файловые операции**: через Filesystem MCP
4. **Тестирование**: через Playwright MCP
5. **База данных**: через Supabase MCP

### CLI алиасы для быстрой работы:
```bash
gstatus          # Статус репозитория
gadd             # Добавить все файлы  
gcommit "msg"    # Коммит с сообщением
gpush            # Отправить в remote
gflow "msg"      # Полный workflow (add→commit→push)
gdev feature     # Управление сессиями разработки
gbackup          # Резервное копирование
gclean           # Очистка репозитория
```

**Фоновые агенты работают 24/7 и исправляют 95%+ ошибок автоматически!** 🚀
**MCP серверы дают суперсилы в разработке! Используй их максимально эффективно!** 🔌
