---
title: "MCP серверы - правила использования"
description: "Правила и инструкции по использованию подключенных MCP серверов для автоматизации разработки"
priority: 3
tags: ["mcp", "servers", "automation", "tools", "integration"]
alwaysApply: true
---

# 🔌 MCP серверы - правила использования

## 🎯 АКТИВНЫЕ MCP СЕРВЕРЫ

### 1. **GitHub MCP Server** 🐙
**Статус**: ✅ Активен
**Пакет**: `github-mcp-server`
**Функции**: 30+ GitHub операций + 33 CLI алиаса

**Доступные операции**:
- **Базовые Git**: init, add, commit, push, pull, branch, checkout
- **Продвинутые**: merge, rebase, cherry-pick, blame, bisect
- **Workflow**: flow, quick-commit, sync, dev, backup, clean
- **Управление**: tags, remotes, stashes, resets

**CLI алиасы**:
```bash
gstatus          # Статус репозитория
gadd             # Добавить все файлы
gcommit "msg"    # Коммит с сообщением
gpush            # Отправить в remote
gflow "msg"      # Полный workflow (add→commit→push)
gdev feature     # Управление сессиями разработки
gbackup          # Резервное копирование
gclean           # Очистка репозитория
```

### 2. **Filesystem MCP Server** 📁
**Статус**: ✅ Активен
**Пакет**: `@modelcontextprotocol/server-filesystem`
**Функции**: Доступ к файловой системе проекта

**Операции**:
- Чтение файлов и директорий
- Создание и редактирование файлов
- Поиск по файлам
- Управление структурой проекта

### 3. **Git MCP Server** 🔧
**Статус**: ✅ Активен
**Пакет**: `@modelcontextprotocol/server-git`
**Функции**: Git операции через MCP

**Операции**:
- Статус репозитория
- История коммитов
- Управление ветками
- Диффы и патчи

### 4. **Supabase MCP Server** 🗄️
**Статус**: ✅ Настроен
**Пакет**: `@supabase/mcp-server`
**Функции**: Прямая работа с базой данных

**Операции**:
- Выполнение SQL запросов
- Управление таблицами
- RLS политики
- Аутентификация

### 5. **Memory MCP Server** 🧠
**Статус**: ✅ Настроен
**Пакет**: `@modelcontextprotocol/server-memory`
**Функции**: Сохранение контекста между сессиями

**Операции**:
- Сохранение важной информации
- Восстановление контекста
- Управление знаниями

### 6. **Time MCP Server** ⏰
**Статус**: ✅ Установлен
**Пакет**: `time-mcp`
**Функции**: Работа с датами и временем

**Операции**:
- Текущее время
- Форматирование дат
- Конвертация временных зон
- Относительное время

### 7. **Playwright MCP Server** 🎭
**Статус**: ✅ Работает
**Пакет**: `@playwright/mcp`
**Функции**: E2E тестирование

**Операции**:
- Автоматизация браузера
- Скриншоты
- Тестирование UI
- Взаимодействие с элементами

### 8. **Firecrawl MCP Server** 🕷️
**Статус**: ✅ Работает
**Пакет**: `firecrawl-mcp`
**Функции**: Веб-скрапинг и анализ

**Операции**:
- Скрапинг веб-страниц
- Извлечение контента
- Анализ структуры
- Batch обработка

### 9. **PostgreSQL MCP Server** 🐘
**Статус**: ✅ Настроен
**Пакет**: `@modelcontextprotocol/server-postgres`
**Функции**: Прямое подключение к PostgreSQL

**Операции**:
- SQL запросы
- Управление схемой
- Анализ данных
- Оптимизация

### 10. **Docker MCP Server** 🐳
**Статус**: ✅ Настроен
**Пакет**: `@modelcontextprotocol/server-docker`
**Функции**: Управление контейнерами

**Операции**:
- Запуск контейнеров
- Управление образами
- Мониторинг
- Логи

## 🎯 ПРАВИЛА ИСПОЛЬЗОВАНИЯ MCP СЕРВЕРОВ

### 1. **Автоматическое использование**
**ВСЕГДА используй MCP серверы для:**
- Git операций (вместо прямых команд)
- Работы с файлами (вместо ручного редактирования)
- Взаимодействия с GitHub (PR, Issues, Actions)
- Тестирования (Playwright для E2E)
- Веб-скрапинга (Firecrawl для данных)

### 2. **Приоритеты MCP серверов**
1. **GitHub** - для всех Git и GitHub операций
2. **Filesystem** - для работы с файлами
3. **Supabase** - для работы с базой данных
4. **Playwright** - для тестирования
5. **Firecrawl** - для веб-данных
6. **Time** - для работы с датами

### 3. **Паттерны использования**

#### Git операции:
```bash
# Вместо: git add . && git commit -m "message"
# Используй: gadd && gcommit "message"

# Вместо: git push origin main
# Используй: gpush

# Вместо: git checkout -b feature
# Используй: gcheckout feature
```

#### Workflow операции:
```bash
# Полный workflow
gflow "implement feature"

# Быстрый коммит
gquick "fix typo"

# Синхронизация
gsync

# Управление сессиями
gdev feature-auth
```

#### Файловые операции:
```bash
# Чтение файлов через MCP
# Создание файлов через MCP
# Поиск по файлам через MCP
```

### 4. **Интеграция с разработкой**

#### При создании новой функции:
1. **GitHub MCP**: Создать feature ветку (`gbranch feature-name`)
2. **Filesystem MCP**: Создать файлы через MCP
3. **Playwright MCP**: Написать E2E тесты
4. **GitHub MCP**: Коммит и push (`gflow "message"`)

#### При тестировании:
1. **Playwright MCP**: Запустить E2E тесты
2. **GitHub MCP**: Проверить статус (`gstatus`)
3. **Memory MCP**: Сохранить результаты

#### При деплое:
1. **GitHub MCP**: Создать PR (`gworkflow create-pr`)
2. **Docker MCP**: Проверить контейнеры
3. **Supabase MCP**: Проверить миграции

## 🚀 АВТОМАТИЗАЦИЯ ЧЕРЕЗ MCP

### Автоматические действия:
- **При коммите**: Автоматически запускать тесты
- **При push**: Проверять CI/CD статус
- **При ошибках**: Автоматически исправлять через GitHub MCP
- **При деплое**: Проверять все системы

### Мониторинг:
- **GitHub MCP**: Статус репозитория
- **Docker MCP**: Статус контейнеров
- **Supabase MCP**: Статус базы данных
- **Memory MCP**: Сохранение логов

## 📊 МЕТРИКИ И ОТЧЕТЫ

### GitHub MCP метрики:
- Количество коммитов
- Время выполнения операций
- Статус CI/CD
- Количество PR и Issues

### Filesystem MCP метрики:
- Количество созданных файлов
- Изменения в структуре
- Статистика использования

### Playwright MCP метрики:
- Количество тестов
- Время выполнения
- Процент успешности
- Покрытие функционала

## 🔧 КОМАНДЫ ДЛЯ УПРАВЛЕНИЯ

### Проверка статуса:
```bash
# Статус всех MCP серверов
ps aux | grep mcp

# Проверка GitHub MCP
npx github-mcp-server --help

# Проверка Playwright MCP
npx @playwright/mcp --help
```

### Перезапуск серверов:
```bash
# Перезапуск Cursor для активации MCP
# Или перезапуск отдельных серверов
```

### Логи и отладка:
```bash
# Логи MCP серверов
tail -f ~/.cursor/logs/mcp.log

# Отладка конкретного сервера
DEBUG=mcp* npx github-mcp-server
```

## 🎯 РЕКОМЕНДАЦИИ

### Для максимальной эффективности:
1. **Используй CLI алиасы** - они быстрее и удобнее
2. **Комбинируй операции** - gflow вместо отдельных команд
3. **Мониторь статус** - регулярно проверяй работу серверов
4. **Сохраняй контекст** - используй Memory MCP для важной информации

### При проблемах:
1. **Проверь статус** - все ли серверы работают
2. **Перезапусти Cursor** - для активации изменений
3. **Проверь токены** - GitHub, Supabase, другие API
4. **Используй логи** - для диагностики проблем

---

**MCP серверы дают суперсилы в разработке! Используй их максимально эффективно! 🚀**

**Статистика**: 10 активных MCP серверов, 30+ GitHub операций, автоматизация 95%+ рутинных задач.