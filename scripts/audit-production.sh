#!/bin/bash
# Скрипт для проверки безопасности только продакшн зависимостей

echo "🔒 Проверка безопасности продакшн зависимостей..."

# Проверяем продакшн зависимости
echo "📦 Проверка продакшн зависимостей..."
if npm audit --audit-level=high --production; then
    echo "✅ Продакшн зависимости безопасны"
else
    echo "❌ Найдены уязвимости в продакшн зависимостях!"
    exit 1
fi

# Показываем информацию о dev-зависимостях (только для информации)
echo ""
echo "🛠️ Информация о dev-зависимостях:"
echo "Уязвимости в dev-зависимостях не критичны, так как они не попадают в продакшн."
echo "Основные уязвимости находятся в Vercel dev-зависимостях:"
echo "- esbuild в @vercel/node (не влияет на продакшн)"
echo "- path-to-regexp в @vercel/remix-builder (не влияет на продакшн)"
echo "- undici в @vercel/node (не влияет на продакшн)"
echo ""
echo "✅ Все критичные зависимости для продакшна безопасны!"
